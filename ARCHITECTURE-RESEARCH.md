# マングローブ海岸保護シミュレーター：技術設計・調査結果

調査日：2026-09-05。設計提案であり、実装承認・性能保証ではない。

## 推奨する方向

**CelerisのNLSW（水深平均の非線形浅水方程式）経路を参考・再利用した小さなWebGPU計算基盤に、根セグメント由来の抵抗と、1種類の細粒土砂・海底更新を結合する。描画も同一GPUDeviceで行う。**

森林帯と海岸の関係を観察する有限領域を、斜め上から自由に回転・拡大できる3D水槽として見せる。全面canvasと小さなDOM操作パネルを使う。サイトのホームページやカード群は作らない。

完全三次元の流体計算は初版に採用しない。理由は「M2では3D流体が不可能」ではなく、森林帯規模・細い根・動く海底・比較2系統を、週末の開発と検証で同時に成立させる難しさにある。

## 要求と成功条件

- MacBook Air M2、16GB、本人の端末のみ。ChromeのWebGPUを第一対象とし、localhostでオフライン起動できる構成。
- 9月6日までの完成を目標、9月9日の授業で使える最小完成版を優先。9月16日になる可能性を前提に機能を増やさない。
- 無料ライブラリ・手元のアセットのみ。サーバー計算、外部API、アカウント、実行時CDNを不要にする。
- 根の有無以外の初期条件・外力・乱数列を固定して比較できる。
- 流速低下→土砂移動→海底変化が同じ状態から出る。木の有無で海岸を直接変形させるコードは禁止。
- 4要件を、海底変化量、堆積量、基準水位の海岸線、低流速域でそれぞれ説明できる。
- 最低30fpsを設計目標とする。ただし本調査では実機実行をしておらず、達成済みではない。

## 検査の範囲と証拠の境界

ローカルの3つのソースZIPからREADME、ライセンス、主要JS/TS/WGSL/Pythonを一時フォルダへ抽出して静的検査した。元ZIP・GLB・Blenderファイルは変更していない。依存関係のインストール、アプリ実行、プロダクションコードの作成はしていない。

GLBはヘッダー、JSON、メッシュ数、三角形数、埋め込み画像寸法、著作者・ライセンス情報を直接確認した。描画による見た目、実寸、根と幹の接続品質は未検証。Blenderファイルの内部も未検証。

公開GitHubと公式資料・研究論文を調査した。GitHubの最新コミットSHA・日付取得は失敗したため、最新コミットや活動停止を断定しない。ZIP内のタイムスタンプはソースの年代を判断する補助情報であり、上流の最終更新日ではない。

共有MEMORYには以前の試作完了記録があるが、今回の作業フォルダはZIPとGLBのみ。以前の検証結果は今回の合格証拠に用いていない。

## 参考資料の独立評価

### Celeris-WebGPU：水理の第一候補、全体移植はしない

[公開リポジトリ](https://github.com/plynett/plynett.github.io)はNLSW、Boussinesq系の波計算とGPU描画を備える。ローカルでも `Pass1.wgsl`、`Pass2.wgsl`、`Pass3_NLSW.wgsl`、`BoundaryPass.wgsl`、土砂関連パスと呼び出しを確認した。

ローカルZIPでの観測：

- `js/main.js` は5,489行。初期化、計算、UI、ファイル処理が密結合。多数のuniform offsetをJS側で直接設定するため、改造時の結合ミスがリスク。
- 土砂パスは単なる着色ではない。Shields数による侵食、沈降、掃流砂に関する計算、海底更新を含む。
- `SedTrans_Pass3.wgsl:121–145` 付近は底面摩擦からせん断を求める。個々の根の幾何を水深に応じて積分する仕組みとは異なる。
- `SedTrans_UpdateBottom.wgsl:85` 付近に侵食・堆積による地形更新、境界付近の更新抑制がある。移植時は単位、保存則、境界処理、硬い底面への到達を再検証する必要がある。ここでは不具合と断定しない。
- `constants_load_calc.js` の時間刻みは基準水深・格子幅から算出される。改造版では流速増加や海底更新を含むCFL条件を監視する。
- 文書の「自動テストなし」に対し、アーカイブにはベンチマーク資料・実行補助が存在する。水理の検証資料が、根・泥・地形連成まで保証するわけではない。
- `Model_Loaders.js` の確認した経路はboxモデル中心。GLBを読み込める完成済み森林レンダラーと見なさない。
- アーカイブは約900MB。大半の例題・地形・画像は今回不要。使用するソースだけを選び、出自を記録する。

採用範囲：水理の離散化・wet/dry処理・造波境界・GPUパス構成。小さな依存閉包として取り出し、元の小規模ケースと比較してから改造する。初版ではBoussinesq/COULWAVE、地図、AIエージェント、地域別データを外す。土砂は式と実装を参考にするが、泥質に合わせた独立モジュールとして検証する。

ライセンス：ローカルLICENSEはMIT、2023 plynett。使用部分に著作権・許諾文を保持する。例題の衛星画像などをMITだからと一括利用しない。保守評価：研究用途の基盤として有力だが、アプリ全体の容易な改造を保証する構造ではない。

### Inkwell WebGPU Water：光学・表現の参考

[公開リポジトリ](https://github.com/siliconjungle/inkwell-webgpu-water)とローカルで、スペクトル波、FFT、浅水流、泡・水面描画を確認。単なる正弦波アニメーションではない。

`webgpu-water-engine.ts` の `hydrostaticPair`、`rusanovX/Y` は浅水流計算。CPU参照テストには静水、段差、wet/dry、隣接セルの保存確認がある。一方、地形は手続き的に生成され、ソース内の `erosion` という変数名は侵食連成の証拠ではない。根の抵抗、浮遊土砂、動的海底の一式は確認できなかった。

Next.js/ReactをUIに使用するが、GPUエンジンは命令的に分離。コードには巨大なWGSL文字列と水表現の密な結合があり、水材質だけの移植も無加工では済まない。保存済みApple/Metalベンチマークは作者の測定で、このM2・森林・2系統計算の性能ではない。

採用範囲：Fresnel反射、吸収、浅瀬の水深表現などの考え方。全FFT・大海原・Next.jsは初版で導入しない。水面の高さと大きな法線は必ず本シミュレーションから得る。装飾波が根の裏側でも同じ強さで通過し、計算結果と矛盾しないようにする。

ライセンス：ローカルMIT、2026 James Addison。コードを転用すれば許諾文を保持。保守評価：比較的新しい描画研究で参照テストはあるが、沿岸地形モデルとしての検証とは別。

### anugaSed：概念参照、直接移植を避ける

[公開リポジトリ](https://github.com/mperignon/anugaSed)はANUGAへのPython追加演算子。ローカルには土砂・植生の2演算子があり、NumPy、SciPy、ANUGA内部APIに依存する。

具体的な懸念：

- Python 2形式の `print`、`apply`、古いNumPy記法が残る。現代Python/ANUGAへの無修正導入は期待できない。ZIP内日時は2017年。
- 植生演算子は直径と間隔から抵抗を作る。根セグメントそのものはない。
- `Vegetation_operator.update_quantities` は各方向の速度二乗で明示的に減速し、その後 `depth = qcell / velocity` と水深を変える。純粋な抵抗sourceが水量を変えないという今回の契約と合わず、ゼロ速度や大きな刻みも要監査。
- 土砂の `update_bed` は1更新の地形変化を0.001mへ制限する。土砂側と同じ交換量で制限されるかを検証せずコピーしてはいけない。

採用範囲：余剰せん断による侵食、沈降、植生抗力を独立sourceとして扱う考え方。数式は一次資料へ戻る。Python→WASM移植、バックエンド化、直接コピーは採らない。

ライセンス：ローカルMIT、2016 Mariela Perignon。コード転用時は許諾文を保持。ANUGA本体が現在利用可能でも、この追加演算子の互換性の証拠にはならない。

### Nice2meetU2のMangrove Tree：条件付きで視覚アセットに採用

[提供元](https://sketchfab.com/3d-models/mangrove-tree-5d4542431caa46f0b6f1feb089fc19dd)。公開ページ本文は取得できなかったが、手元のGLBには同じURL、作者Nice2meetU2、CC-BY-4.0が明記されている。

実測：6,158,904 bytes、3 meshes、9 nodes、計4,268三角形、3 materials、8枚の埋め込みPNG。画像寸法は256×1024〜1024×1024。圧縮ファイル容量よりGPUテクスチャ展開量は大きいが、森林の全個体で画像を共有できる。

メッシュ別三角形数は2,909、1,231、128。`Plane_2` を含むため地面パーツの可能性があり、後続の視覚検査で分類する。元ZIPにはBlenderファイルと画像があり、根の編集素材になり得る。

根一本ごとの独立ノードは確認できない。従って「GLB全体を物理コライダー化」で要求を満たしたとしない。見える主要な根をトレースしたセグメントデータをテンプレートとして作り、同じ変換で表示と物理を配置する。整合しなければ元アセットの派生コピーで根を置き換え、幹・樹冠を再利用する。元ファイルは維持する。

葉の透過、両面材質、地面パーツ、軸、単位、根の水没状態はレンダリング検査待ち。アセットの名称だけから樹種・根の科学的正確さは保証しない。

クレジット案：Mangrove Tree — Nice2meetU2 / Sketchfab / CC BY 4.0。派生した場合は根形状・縮尺・材質などの変更も記載。[CC BY 4.0の条件](https://creativecommons.org/licenses/by/4.0/)に従い作者、リンク、ライセンス、変更を表示する。

## 追加候補を比較した結果

| 候補 | 役割・不採用理由 |
|---|---|
| [XBeach](https://github.com/openearth/xbeach) | 波・土砂・地形・植生を扱う科学的参照として強い。ブラウザへの週末移植は範囲過大。モデル式・検証例を参照し、ランタイムには組み込まない。コード転用ならLGPLの適用範囲を別途確認。 |
| [ANUGA core](https://github.com/GeoscienceAustralia/anuga_core) | 三角形格子の浅水モデル。Python主体で、今回のGPU直接描画への接続費用が増える。外部検算候補だが初版の必須依存にしない。 |
| [WebGPU-Ocean](https://github.com/matsuoka-601/WebGPU-Ocean) | MLS-MPM/SPHの3D流体。READMEに時間刻みの不安定性とMacのSPH問題の記載。海岸形態・泥輸送・根の検証は別途必要。MITだが今回の基盤には採らない。 |
| [GPU-Gang/WebGPU-Erosion-Simulation](https://github.com/GPU-Gang/WebGPU-Erosion-Simulation) | stream-powerによる地形生成と描画が中心。往復する沿岸波・wet/dry・根による運動量散逸の代替ではない。BSD-3-Clause、性能技法の参考に限定。 |
| [COAWST_rh論文](https://gmd.copernicus.org/articles/16/5847/2023/) | Rhizophoraの根の高さ別構造と乱流を考慮するモデル。今回の根表現と省略範囲を決める科学的参照。大型研究システム全体を導入しない。 |

追加候補は公開文書による選別で、提供された3ZIPと同水準の全ソース検査はしていない。今回の条件でCelerisの限定再利用より明確に有利な完成品は見つからなかった。

## 計算モデル

### 領域と波

初期案は岸沖96m×沿岸64m、格子192×128、格子幅0.5m。森林帯幅15〜25m、40〜80本程度から始める。これは設計開始値であり生態調査値ではない。斜面は緩い泥質干潟とし、基準の低潮・高潮の間に主要な根域が位置するよう配置する。

主対象は浅海の長周期波と潮流。初期の波周期案8〜12秒、沖側水深1〜2m。波高は格子・水深と整合する範囲を試作で決める。短周期深海波、根の周りの鉛直渦、砕波の巻き込みは解像しない。NLSWの適用性はkh≪1と波形・減衰の検算で確認する。[Celeris原論文](https://arxiv.org/abs/1611.05984)は分散性を含む経路との違いを確認する参照になる。

有限の見た目でも、四方を完全反射壁にはしない。沖側は入射と流出を扱う造波・吸収境界、側方は定義済みの境界、陸側はdry terrain。吸収域は評価領域から外す。壁反射をマングローブ効果と混同しない。潮位変更は両系統の沖側境界で同じ滑らかな時間系列として与える。

### 保存する状態

| 状態 | 単位・用途 |
|---|---|
| h | 水深 [m] |
| qx, qy | 単位幅流量 hu, hv [m²/s] |
| zb | 海底標高 [m]、η=zb+h |
| ms=hC | 単位面積の浮遊土砂質量 [kg/m²] |
| mb | 侵食可能な底質質量 [kg/m²]、非侵食性基盤との差に対応 |
| 根の高さ別投影面積 | 根セグメントをセルへ割り当てた幾何情報 |

水深平均NLSWを有限体積法で進める。静水を保つ地形source、非負水深、wet/dry、底面摩擦を必須とする。最小CPU参照を持ち、WGSLとの一致を検査する。CPU/GPUが一致しても同じ誤りを共有し得るため、解析解・保存則・格子収束も別に検査する。

### 個々の根から抵抗を作る

共通の `RootSegment` 定義に端点、半径、tree IDを持たせる。視覚メッシュと物理場の生成が同じ定義・変換を参照する。根を太く見せるために物理半径を無言で増やさない。

各セル内のセグメントを海底と水面でクリップし、水没長さと流れに対する投影面積を得る。高さ別に事前集計すれば、潮位による水没割合を毎ステップ安価に更新できる。初版は固定本数・固定配置でよく、毎フレーム全根を全セルと照合しない。

等方近似で説明する場合、a=A_submerged/(cellArea×h) [1/m] として、加速度は −0.5 Cd a |u|u。運動量sourceはそれにhを掛ける。方向依存を入れる際は斜め根の投影と抵抗方向も整合させる。Cdは経験的パラメータで、普遍的な定数として扱わない。

抵抗sourceは半陰的または解析的減衰で更新し、抵抗だけで流れが反転・加速したり、水量が増えたりしないようにする。セル内の水没根面積の和を保存する割当てにし、格子幅を変えても森林の総抵抗面積が変わらないことを確認する。

これは**個々の根が寄与するサブグリッド抵抗モデル**であり、各根の表面流れを解くCFDではない。0.5m格子で数cmの根の後流を再現したと主張しない。根の間の大きな流路は格子で表現できるが、小さな隙間は平均化される。

[Yoshikaiらの研究](https://gmd.copernicus.org/articles/16/5847/2023/)は高さ別構造と乱流の重要性を示す。初版では高さ別幾何を取り入れ、鉛直流速分布と根由来乱流の精密モデルは省く。

### 土砂、侵食、堆積

泥質干潟に対して砂用輸送能力式を名前だけ変えて使わない。初版は1種類の細粒・凝集性底質の近似とし、Partheniades型の余剰せん断侵食と、Krone型の沈降・堆積を候補式として用いる。これも経験式であり、実測校正なしの定量予測ではない。[HEC-RAS侵食説明](https://www.hec.usace.army.mil/confluence/rasdocs/rassed1d/1d-sediment-transport-technical-reference-manual/cohesive-transport/krone-and-partheniades-methods/erosion)、[堆積説明](https://www.hec.usace.army.mil/confluence/rasdocs/d2sd/ras2dsedtr/6.6/model-description/deposition)。

基本契約：

- ∂ms/∂t + div(u ms) = div(h K grad C) + E − D。
- E = M max(τb/τce − 1, 0)、D = ws C max(1 − τb/τcd, 0)。E,Dはkg/m²/s。これは採用候補の簡略closureで、唯一の正解ではない。
- Δmb = −Δms_exchange、Δzb = Δmb/[ρs(1−p)]。
- M、τce、τcd、ws、pは出典と選定理由をパラメータ台帳へ記録する。未検証の値で海岸後退m/年を表示しない。
- 水中濃度Cとmsを混同しない。移流は水理の面フラックスと整合し、流出量を供給セルの土砂量で制限する。dryセルへ土砂を消さず、底へ保存移送するか残留質量として保持する契約を一つに固定する。
- 堆積は手持ちms、侵食は手持ちmbまで。同じ制限済み交換量を双方に適用する。
- 海底だけ滑らかにして土砂質量を失わない。急斜面緩和を導入する場合もセル間の保存的移送とする。

底面せん断τbは、水深平均流速と底面摩擦から近似する。植生抵抗を底面摩擦へ単純加算すると、根への力をすべて土砂を削る力と誤解するので分ける。一方、根由来乱流がゼロという意味ではない。今回の省略は局所洗掘を過小評価し得る。[植生乱流と堆積の研究](https://www2.whoi.edu/staff/dralston/wp-content/uploads/sites/147/2024/01/DeitrickEtal_FrontMarSci_2023_vegTurbDepo.pdf)。

土砂供給は初期浮遊土砂と沖側流入濃度として明示する。供給がなければ、森林を置くだけで新しい土砂は生まれない。

海底更新後、固定ηでhだけ減らす処理を無条件に使わない。初版の浅水近似では交換ステップ中の水柱体積hを維持してzbを更新し、ηの変化を次の水理計算へ返す方針とする。間隙水・混相体積は省略。変化量を小さく保ち、静水、堆積、wet/dryで人工波と体積収支を検査する。

### 海岸線と生物の避難場所

瞬時の水際はηとzbで決まり、潮汐だけでも移動する。侵食による海岸線は**固定基準水位とzbの交線**で測る。初期線と現在線を重ね、同じ沿岸区間の平均後退量と海底差分を出す。これにより潮位上昇を侵食と誤認しない。

避難場所は水没域に限定した時間平均流速・せん断の低い領域として示す。生物の生存率や個体数増加は計算しない。任意の魚表示があっても、その行動は説明用であり生態学的検証の証拠にしない。主証拠は低エネルギー領域の実測マップと面積。

地下根の土壌補強・引張強度は初版の対象外。海岸線安定化は流れと土砂を介した効果として説明し、機械的な土壌固定まで再現したとしない。

## 同一条件の比較

2つの独立したState A/Bを保持し、初期海底・水量・土砂・境界・乱数seedを複製。Aは根抵抗あり、Bは抵抗ゼロ。片方の計算結果を加工してもう片方を作らない。

時間刻みは両者で必要な安全値の小さい方とし、同一の物理時刻で進める。波生成はframe数や実時間に依存させない。計算予算が足りなければ両者を一緒に遅らせる。

初版は1つの大きな水槽をA/B切替で表示し、共通カメラ・共通カラースケール・数値比較を維持する。分割表示は同じデータを2 viewportで描く追加機能。水槽2つを物理的に隣接させて相互作用させない。

wave/tide変更は双方へ同時適用できるが、正式な比較は条件変更後の共通リセットから行う。初期過渡と定常評価区間を分ける。

## 時間加速をどう扱うか

第一選択は、安定なdtを保ったまま表示1フレームの計算ステップ数を増やす。計算時刻を表示し、30fpsと30倍速を混同しない。

目安としてdx=0.5m、h=2m、|u|=1m/s、Courant=0.2なら、単純な1方向見積りdtは約0.018秒。2方向CFLではさらに小さくなり得る。数分で数か月を忠実に回すことは、この数字からも別問題と分かる。

地形だけを100倍動かす実装は採用しない。形態加速は水理と地形の時間尺度を分ける近似であり、浮遊・底質交換の収支と境界供給を含む専用の検証が必要。[XBeachのmorfac説明](https://xbeach.readthedocs.io/en/latest/xbeach_manual.html#bottom-updating)も適用条件を設けている。

初版は全状態の早送り＋地形差分色＋断面表示を優先する。時間内に見える変化が足りなければ、比較したい長さの計算を先に走らせる。形態加速はM=1との感度検査を通した小さな倍率に限る追加段階とする。「数分で長期間」は保証せず、表示される物理時間を正直に保つ。

## 描画とライブラリ

推奨ランタイムはTypeScript＋Vite＋raw WebGPU/WGSL。UIは素のHTML/CSS。React、Next.js、R3F、汎用剛体エンジンは不要。Viteは開発・ローカル配布用で、科学計算には関与しない。[Vite公式](https://vite.dev/guide/)、[Chrome WebGPU公式](https://developer.chrome.com/docs/web-platform/webgpu/overview)。

raw WebGPUを選ぶ理由は、既存WGSLと状態テクスチャを同じGPUDevice上で直接描画に渡すため。Three.js描画との毎フレームreadback/reuploadや、別GPUDeviceの資源共有問題を避ける。汎用エンジンよりモデル描画の実装負担は増えるため、描画対象を地面・水・幹葉・根に限定する。

Three.jsは必要なら[GLTFLoader](https://threejs.org/docs/pages/GLTFLoader.html)をCPU側の読み込み専用として利用し、単純な静的メッシュ・材質へ正規化する。GLTFLoaderがraw WebGPU用描画を作ってくれるわけではない。Blenderで派生GLBの根・単位を調整する。実装時には使用するMITライブラリのバージョンとLICENSEを固定する。

Three.js WebGPURendererは一般の3Dには魅力的だが、外部GPU状態との接続方式を確かめず採用しない。WebGL fallbackがあってもWGSL計算まで代替するわけではない。[公式WebGPURenderer](https://threejs.org/docs/pages/WebGPURenderer.html)。

水面はη、海底はzb、濁りはCから描画。水面の大きな波を別FFTで上書きしない。小さな法線の細部は装飾に限定。濁った水で根が見えない問題は、透明度を物理結果と別に操作する観察モードと根overlayで解く。透明度を下げても物理抵抗は変わらない。

樹木は形状・画像を共有しinstance描画。根はテンプレートセグメントから描画。海岸全体→根付近へズームできる。水槽の側面で地形差を読むことも可能にする。高価な多重反射、ボリューム雲、大量の泡、海中カメラは後回し。

## 実装担当へのモジュール契約

最小構成は simulation / rendering / diagnostics / ui と共通scenario定義程度でよい。

1. Scenario：SI単位、初期地形、初期土砂、基盤、根テンプレート、境界、seed。
2. Hydro：保存的面flux、地形source、wet/dry、CFL、底面摩擦。
3. Roots：表示と共有した幾何→高さ別局所抵抗。水量を書き換えない。
4. Sediment：移流とE/D、正値制限、底質との厳密な交換。
5. Morphology：同じ交換量からzb更新。場を次ステップへ返す。
6. Comparison：同一外力、同期時刻、2状態、リセット。
7. Rendering：状態を読む。物理状態を視覚都合で書き換えない。
8. Diagnostics：体積・質量収支、root面積、NaN、波高、平均流速、地形差、基準海岸線、処理時間。

状態はGPUでping-pongし、CPU読み戻しは小さな診断値を数Hz程度に制限。物理の刻みは描画と分離。背景タブでは停止し、復帰時の巨大dtを防ぐ。device lostでは停止理由と再初期化手段を表示する。

## 性能予算と検証基準

目標：1440×900程度、pixel ratio上限1〜1.5、30fps以上、比較2状態、10分継続。まず128²で計測し、その後192×128、必要なら256²へ。これらは未計測の予算である。描画fps、1ステップ時間、物理秒/実秒、長時間の発熱後の性能を分けて記録する。

RGBA32F 256²テクスチャは1MiB。多数の状態でも16GB全体が直ちに制約にはならないが、GPU帯域、dispatch数、半透明の重なりが制約になり得る。低速時は描画解像度・影・植物表示詳細を先に落とし、最後に格子を変更して比較をやり直す。格子削減後に同じ定量結果と称さない。

実装の必須合格検査：

| 検査 | 確認すること |
|---|---|
| 静水・段差・斜面 | 波や流れが勝手に生まれない |
| 閉領域の水量 | 境界fluxを含め保存。dragのみでh不変 |
| 抵抗だけの一様流 | 理論的減衰と一致。逆転・エネルギー増加なし |
| 根の変換・水没 | 移動/回転/scale/潮位が表示と抵抗へ同じように反映 |
| 根面積の格子比較 | 128系と倍解像度で総面積保存 |
| 土砂の移流・沈降 | 非負、静水沈降の解析挙動、初期+流入−流出=浮遊+底質 |
| E/D・基盤・干出 | 交換制限後も収支一致、底質不足で侵食停止 |
| 無根A/B | 同じ計算条件なら両者が一致 |
| 植生比較 | 林内流速、海底差分、堆積、低流速面積を同一時刻で取得 |
| 解像度・dt半減 | 保護効果の符号が数値拡散だけで変わらない |
| 潮位だけ変更 | 基準海岸線はzbが変わらなければ移動しない |
| 高波・低水位・長時間 | NaN、負質量、破綻を起こさず設定範囲内で継続 |

初期の数値合格値案：短い閉系試験の水量相対誤差1e-4以下、土砂1e-3以下。ゼロに近い量は別途絶対誤差で扱う。倍解像度で主要な非ゼロ集計指標の差10%以内を初期目安とし、未達なら精度限界を記載。これらは設計上の基準であり検証結果ではない。

「根があると堆積量が増える」だけをテストの正解にしない。土砂なしでは堆積ゼロ、無流では根の有無による差なし、強い外力では保護が不十分になることも認める。

## 開発段階と週末の判断

以下は実装承認後の順序。時間は作業枠の案で、工数見積りの保証ではない。

1. **最初の2時間：実機と水理の成立確認。** 参照ソースを固定、最小NLSWをM2/Chromeで動かす。静水・造波・wet/dry・計測。ここが通らなければ森林・材質へ進まない。
2. **土曜前半：根の因果を成立させる。** 1根→1木→帯。共通幾何、浸水率、減衰試験、無根との同期比較。単色表示でよい。
3. **土曜後半〜日曜前半：土砂と海底。** まず移流・静水沈降、次にE/D・地形・干出。水量と土砂収支を数値で確認。この段階が最大の不確実性。
4. **日曜中盤：4要件を一つの実験にする。** 潮位固定または緩変化の共通外力、供給土砂、森林帯、海岸線差分、低エネルギー域。見える差の原因を説明する。
5. **日曜後半：3Dと発表安定性。** アセット確認・instance化、泥色・濁り・根overlay、小パネル、リセット、10分試験、ネット切断起動。操作手順と限界表示を整える。

日曜正午までに保存的なE/Dと海底更新が通らなければ、写実表現、分割表示、魚アニメーション、形態加速を切る。それでも連成が成立しなければ4要件完成とは報告せず、9月9日まで検証を続ける。見た目の侵食アニメーションで穴埋めしない。

## 技術上の主要リスクと設計を変える条件

| リスク | 対応・変更条件 |
|---|---|
| NLSWが必要な波を再現しない | 長周期・浅い設定でも検算不良なら、限定Boussinesq経路を再評価。先に見た目の波を足さない |
| 根の後流・局所洗掘を省略 | 「格子平均の保護」を説明。課題が個根の渦を要求する場合は別の小領域3Dモデルへ範囲変更が必要 |
| 泥の経験係数に結果が依存 | 台帳と感度試験。校正なしで実海岸の予測量を主張しない |
| 数値拡散を植生効果と誤認 | 同一外力、無根baseline、倍解像度、波高の測点比較 |
| morphodynamicsが不安定 | 保存的制限と小さなdt/交換量。恣意的clampで失った質量を隠さない |
| raw描画の工数増 | 最小材質、限定GLB、共通根テンプレート。最初から汎用エンジンを作らない |
| 濁りで説明対象が見えない | 根・流速・地形差の観察overlay。物理量と観察設定を分離 |
| 時間加速が成り立たない | 実計算早送りと差分観察へ限定。季節・年スケールの予測を約束しない |

## 最終判断

この計画は「すべての現象を最も精密に解く」ことではなく、**要求された因果を保存則と検証で支え、説明できる範囲に絞って実装する**ことを優先する。最も重要な次の証拠は、見栄えの良い水面ではなく、M2で動く小領域の水理・根抵抗・土砂収支の成立である。

実装開始にはユーザーの明示的な依頼が必要。本調査ではその段階へ進んでいない。
